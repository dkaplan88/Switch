<%= form_for(@devise) do |f| %>
  <% if @devise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@devise.errors.count, "error") %> prohibited this devise from being saved:</h2>

      <ul>
      <% @devise.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
